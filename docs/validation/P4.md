# P4 — OSS documentation and safe demo validation

Date: 2026-08-14
Platform: Windows 11, Node.js 24.18.0, npm 11.16.0

## Implemented

- Reworked the README around the read-only forensic safety model, quick start, confidence semantics, compatibility status, limitations, and privacy guidance.
- Added contributor rules for fixtures, parsers, confidence labels, bounded processing, and the read-only invariant.
- Added security guidance for local evidence, IndexedDB, screenshots, exports, and private vulnerability reporting.
- Added an explicitly signed synthetic demo result and a localized demo loader.
- Added synthetic-only screenshots for the overview, evidence links, and evidence detail flows.
- Added localized evidence interpretations so the Korean UI does not expose English explanatory text.

## Validation

| Check | Result |
| --- | --- |
| `npm run fixtures:generate` | PASS; deterministic generated fixtures had no tracked diff |
| `npm run lint` | PASS |
| `npm run build` | PASS |
| `npm test` | PASS; 19/19 |
| `npm run test:fixtures` | PASS; 15/15 |
| `npm pack --dry-run --json --ignore-scripts` | PASS; 59 entries, 553,180-byte package estimate |
| `git diff --check` | PASS |
| Browser smoke test | PASS; Korean synthetic overview, evidence list, and reference detail inspected |
| Screenshot review | PASS; only committed synthetic identifiers were visible |

The first documentation regression run failed because the new test checked a nonexistent `fixtureSignature` property. The generated artifact and UI consistently use `signature`. The assertion was corrected to the actual public artifact contract, then the complete P4 sequence passed.

## Safety and evidence notes

- The demo is marked `synthetic-demo-v1` and uses the directory name `synthetic-kahadb-demo`.
- Screenshots contain no customer, production, personal, credential, or proprietary product data.
- Runtime compatibility remains **Not verified** because no broker-generated redistributable fixture is committed.
- The UI continues to avoid broker startup, recovery, store writes, uploads, and automated outage-cause claims.

# P2 — Evidence correlation

PHASE: P2

STATUS: PASS

Implemented:

- Correlation model for messages, ACK/remove records, subscriptions, transactions, and Advisories.
- Evidence references with source file, offset, parsed record ID, and nearby raw strings where available.
- Explicit interpretation boundaries for missing ACK and transaction terminal records.
- Localized, sortable, paginated Evidence Links UI with drill-down into files, structured records, raw strings, and heuristic candidates.
- Scanner cache version increment so older results without correlation data are not reused.

Validation:

```text
npm run fixtures:generate  PASS
npm run lint               PASS
npm run build              PASS
npm test                   PASS (17/17)
npm run test:fixtures      PASS (15/15)
```

Fixture validation:

- Queue message `ID:MESSAGE:1` links to `ORDERS`, its add record, matching remove/ACK record, file, and offsets.
- Durable subscription `client-a:prices` links to Topic `PRICES` from one parsed record.
- Local transaction `ID:CLIENT:1/42` links its message and commit records.
- Structured Advisory message `ID:ADVISORY:1` links to `ActiveMQ.Advisory.TempQueue`.
- The message without a matching ACK reports `Not observed` and preserves the non-proof warning.

Regression:

- All P0/P1 fixture comparisons remain deterministic.
- Structured parsing, malformed/corrupt behavior, existing render checks, sorting, and pagination remain covered.
- All fixture source hashes remain unchanged across scans.

Commit:

```text
feat: correlate ActiveMQ store evidence across messages and records
```

Known limitations:

- Correlation is bounded by selected files, retained journal history, and parser support.
- OpenWire bodies are not decoded, so structured Advisory payload semantics remain unknown.
- Missing evidence is never promoted into a causal or negative assertion.

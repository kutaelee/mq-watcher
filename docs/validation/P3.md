# P3 — Distribution and installation

PHASE: P3

STATUS: PASS

Implemented:

- `mq-watcher` CLI with help/version/port options and loopback-only HTTP binding.
- Packaged static assets and SSR handler served without adding a store upload API.
- Publish-ready npm metadata and explicit package file allowlist.
- Windows/Ubuntu GitHub Actions matrix for install, lint, build, full tests, and fixture tests.
- Automated CLI smoke test for HTML, worker modules, loopback binding, and method rejection.

Validation:

```text
npm ci                     PASS
npm run build              PASS
npm test                   PASS (18/18)
npm run test:fixtures      PASS (15/15)
node bin/mq-watcher.mjs --help     PASS
node bin/mq-watcher.mjs --version  PASS (0.1.0)
npm pack --dry-run --json  PASS (58 entries, 546,738-byte tarball estimate)
npm audit --omit=dev       PASS (0 production vulnerabilities)
```

CLI smoke:

- Bound to `127.0.0.1` on an available port.
- `/` returned the MQ Watcher HTML.
- `/store-scanner.worker.js` returned the worker module.
- `POST /` returned 405.
- Test teardown closed the server; no persistent helper was left running.

Regression:

- Structured parser, evidence correlation, all golden fixtures, render checks, and source hash invariants passed after a clean `npm ci`.

Commit:

```text
build: add reproducible CLI distribution and CI
```

Known limitations:

- No npm publish was performed; `npx mq-watcher` becomes registry-backed only after separate approval and publication.
- Linux and Node 22.13 are configured in CI but were not executed in this local Windows validation.
- Development dependencies currently report 10 audit findings; the shipped production dependency graph reports zero.
